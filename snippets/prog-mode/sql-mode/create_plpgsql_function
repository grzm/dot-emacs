# key: cfp
# --
CREATE FUNCTION
${1:function_name}(${2:parameter_list})
RETURNS ${3:type}
${STABLE}
${STRICT} LANGUAGE PLPGSQL AS $body$
DECLARE
BEGIN
$0
END
$body$;
COMMENT ON FUNCTION $1($2) IS
'';
