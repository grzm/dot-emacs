CREATE FUNCTION
${1:function_name}(${2:parameter_list})
RETURNS ${3:type}
${STABLE}
${STRICT} LANGUAGE ${sql} AS $body$
/**
 *
 * @author     Michael Glaesemann <grzm@seespotcode.net>
 * @since      `(format-time-string "%Y-%m-%d" (current-time))`
 *
 * @private
 *
 */
$0
$body$;
